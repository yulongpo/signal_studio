# 模块详细设计说明书

| 元数据项 | 内容 |
|---|---|
| 文档编号 | SS-DES-MOD-001 |
| 文档名称 | 模块详细设计说明书 |
| 项目名称 | Signal Studio / Signal Platform |
| 文档版本 | V1.0.0 |
| 基线版本 | BL1.0 |
| 状态 | 已批准 |
| 内容类型（meta.contentType） | Conceptual |
| 编制日期 | 2026-07-22 |
| 适用阶段 | 详细设计与实现 |
| 输入来源 | 总体架构、API 目录 |
| 本版变更 | 定义十模块责任、API、线程、依赖和非目标 |

## 设计总则

模块按公开契约、私有实现、适配器和测试夹具四区组织；下列每个模块均可独立构建、安装和测试。

## SignalCore

- 责任：LIB-CORE-001 Result与错误、LIB-CORE-002 单位与版本、LIB-CORE-003 日志与配置、LIB-CORE-004 路径与校验、LIB-CORE-005 序列化与能力探测。
- 公共 API：API-CORE-001, API-CORE-002, API-CORE-003, API-CORE-004, API-CORE-005, API-CORE-006, API-CORE-007；命名空间 `signal::core`。
- 数据/线程：不可变请求优先；UI 仅主线程，IO/计算/推理均由 TaskRuntime 调度；只读视图明确生命周期。
- 依赖：无公共模块依赖；第三方仅私有 Adapter。
- 错误/性能：统一 `Result<Error>`、追踪 ID、资源预算和基准；不得静默回退。
- 测试：单元、契约、适配器、性能、包消费；复用目标：全部应用、Headless CLI。
- 发布：`signalcore-runtime/dev`，SemVer 1.x；非目标：应用页面、品牌和应用专属工作流。

## SignalCompute

- 责任：LIB-CMP-001 设备探测、LIB-CMP-002 后端选择、LIB-CMP-003 计划缓存、LIB-CMP-004 内存池、LIB-CMP-005 一致性与降级。
- 公共 API：API-COMPUTE-001, API-COMPUTE-002, API-COMPUTE-003；命名空间 `signal::compute`。
- 数据/线程：不可变请求优先；UI 仅主线程，IO/计算/推理均由 TaskRuntime 调度；只读视图明确生命周期。
- 依赖：SignalCore；第三方仅私有 Adapter。
- 错误/性能：统一 `Result<Error>`、追踪 ID、资源预算和基准；不得静默回退。
- 测试：单元、契约、适配器、性能、包消费；复用目标：全部计算型应用。
- 发布：`signalcompute-runtime/dev`，SemVer 1.x；非目标：应用页面、品牌和应用专属工作流。

## SignalData

- 责任：LIB-DATA-001 信号容器、LIB-DATA-002 描述符与范围、LIB-DATA-003 零拷贝切片、LIB-DATA-004 内存映射、LIB-DATA-005 格式适配与校验。
- 公共 API：API-DATA-001, API-DATA-002, API-DATA-003, API-DATA-004, API-DATA-005；命名空间 `signal::data`。
- 数据/线程：不可变请求优先；UI 仅主线程，IO/计算/推理均由 TaskRuntime 调度；只读视图明确生命周期。
- 依赖：SignalCore；第三方仅私有 Adapter。
- 错误/性能：统一 `Result<Error>`、追踪 ID、资源预算和基准；不得静默回退。
- 测试：单元、契约、适配器、性能、包消费；复用目标：Signal Studio、Signal Generator、Dataset Builder、Dataset Manager、Model Trainer、Inference Studio。
- 发布：`signaldata-runtime/dev`，SemVer 1.x；非目标：应用页面、品牌和应用专属工作流。

## SignalTaskRuntime

- 责任：LIB-TASK-001 队列与优先级、LIB-TASK-002 DAG依赖、LIB-TASK-003 暂停恢复取消、LIB-TASK-004 进度日志指标、LIB-TASK-005 恢复与历史。
- 公共 API：API-TASK-001, API-TASK-002, API-TASK-003, API-TASK-004；命名空间 `signal::task`。
- 数据/线程：不可变请求优先；UI 仅主线程，IO/计算/推理均由 TaskRuntime 调度；只读视图明确生命周期。
- 依赖：SignalCompute, SignalCore；第三方仅私有 Adapter。
- 错误/性能：统一 `Result<Error>`、追踪 ID、资源预算和基准；不得静默回退。
- 测试：单元、契约、适配器、性能、包消费；复用目标：全部应用、自动化测试运行器。
- 发布：`signaltaskruntime-runtime/dev`，SemVer 1.x；非目标：应用页面、品牌和应用专属工作流。

## SignalDSP

- 责任：LIB-DSP-001 FFT/PSD/STFT、LIB-DSP-002 滤波、LIB-DSP-003 重采样、LIB-DSP-004 频移与包络、LIB-DSP-005 统计与同步。
- 公共 API：API-DSP-001, API-DSP-002, API-DSP-003, API-DSP-004, API-DSP-005；命名空间 `signal::dsp`。
- 数据/线程：不可变请求优先；UI 仅主线程，IO/计算/推理均由 TaskRuntime 调度；只读视图明确生命周期。
- 依赖：SignalData, SignalCompute, SignalCore；第三方仅私有 Adapter。
- 错误/性能：统一 `Result<Error>`、追踪 ID、资源预算和基准；不得静默回退。
- 测试：单元、契约、适配器、性能、包消费；复用目标：Signal Studio、Signal Generator、Dataset Builder、Model Trainer、Inference Studio。
- 发布：`signaldsp-runtime/dev`，SemVer 1.x；非目标：应用页面、品牌和应用专属工作流。

## SignalVisualization

- 责任：LIB-VIS-001 时域与导航、LIB-VIS-002 频谱与PSD、LIB-VIS-003 瀑布与时频、LIB-VIS-004 星座与眼图、LIB-VIS-005 视口覆盖层导出。
- 公共 API：API-VIS-001, API-VIS-002, API-VIS-003, API-VIS-004, API-VIS-005, API-VIS-006；命名空间 `signal::visualization`。
- 数据/线程：不可变请求优先；UI 仅主线程，IO/计算/推理均由 TaskRuntime 调度；只读视图明确生命周期。
- 依赖：SignalData, SignalTaskRuntime, SignalCore；第三方仅私有 Adapter。
- 错误/性能：统一 `Result<Error>`、追踪 ID、资源预算和基准；不得静默回退。
- 测试：单元、契约、适配器、性能、包消费；复用目标：Signal Studio、Signal Generator、Model Evaluator、Inference Studio。
- 发布：`signalvisualization-runtime/dev`，SemVer 1.x；非目标：应用页面、品牌和应用专属工作流。

## SignalModelRuntime

- 责任：LIB-MODEL-001 模型注册、LIB-MODEL-002 前后处理、LIB-MODEL-003 推理会话、LIB-MODEL-004 设备与批处理、LIB-MODEL-005 结果与性能。
- 公共 API：API-MODEL-001, API-MODEL-002；命名空间 `signal::model`。
- 数据/线程：不可变请求优先；UI 仅主线程，IO/计算/推理均由 TaskRuntime 调度；只读视图明确生命周期。
- 依赖：SignalData, SignalCompute, SignalTaskRuntime, SignalCore；第三方仅私有 Adapter。
- 错误/性能：统一 `Result<Error>`、追踪 ID、资源预算和基准；不得静默回退。
- 测试：单元、契约、适配器、性能、包消费；复用目标：Signal Studio、Model Trainer、Model Evaluator、Inference Studio。
- 发布：`signalmodelruntime-runtime/dev`，SemVer 1.x；非目标：应用页面、品牌和应用专属工作流。

## SignalDataset

- 责任：LIB-DSET-001 清单索引、LIB-DSET-002 标签元数据、LIB-DSET-003 分片版本、LIB-DSET-004 划分统计查询、LIB-DSET-005 格式适配缓存。
- 公共 API：API-DSET-001, API-DSET-002；命名空间 `signal::dataset`。
- 数据/线程：不可变请求优先；UI 仅主线程，IO/计算/推理均由 TaskRuntime 调度；只读视图明确生命周期。
- 依赖：SignalData, SignalTaskRuntime, SignalCore；第三方仅私有 Adapter。
- 错误/性能：统一 `Result<Error>`、追踪 ID、资源预算和基准；不得静默回退。
- 测试：单元、契约、适配器、性能、包消费；复用目标：Dataset Builder、Dataset Manager、Model Trainer、Model Evaluator。
- 发布：`signaldataset-runtime/dev`，SemVer 1.x；非目标：应用页面、品牌和应用专属工作流。

## SignalPluginSDK

- 责任：LIB-PLG-001 插件元数据、LIB-PLG-002 版本化ABI、LIB-PLG-003 服务句柄、LIB-PLG-004 权限隔离、LIB-PLG-005 模板与契约测试。
- 公共 API：API-PLG-001, API-PLG-002, API-PLG-003；命名空间 `signal::plugin`。
- 数据/线程：不可变请求优先；UI 仅主线程，IO/计算/推理均由 TaskRuntime 调度；只读视图明确生命周期。
- 依赖：SignalData, SignalTaskRuntime, SignalCore；第三方仅私有 Adapter。
- 错误/性能：统一 `Result<Error>`、追踪 ID、资源预算和基准；不得静默回退。
- 测试：单元、契约、适配器、性能、包消费；复用目标：全部宿主、Headless CLI、自动化测试运行器。
- 发布：`signalpluginsdk-runtime/dev`，SemVer 1.x；非目标：应用页面、品牌和应用专属工作流。

## SignalWorkbench

- 责任：LIB-WB-001 应用框架、LIB-WB-002 Dock与Inspector、LIB-WB-003 任务结果中心、LIB-WB-004 命令与设置、LIB-WB-005 诊断主题DPI。
- 公共 API：API-WB-001, API-WB-002, API-WB-003, API-WB-004；命名空间 `signal::workbench`。
- 数据/线程：不可变请求优先；UI 仅主线程，IO/计算/推理均由 TaskRuntime 调度；只读视图明确生命周期。
- 依赖：SignalVisualization, SignalTaskRuntime, SignalCore；第三方仅私有 Adapter。
- 错误/性能：统一 `Result<Error>`、追踪 ID、资源预算和基准；不得静默回退。
- 测试：单元、契约、适配器、性能、包消费；复用目标：全部桌面工具。
- 发布：`signalworkbench-runtime/dev`，SemVer 1.x；非目标：应用页面、品牌和应用专属工作流。

## 参考资料

- 原始材料：`../references/`（交付目录之外，只读输入）
- 平台任务提示词（平台化架构版）

## 未决事项

- 无阻断性未决事项；正文中的建议值和待确认项继续按其原状态追踪，不因文档获批而视为已实施。

## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| V1.0.0 | 2026-07-22 | 建立并自动审核通过平台化开发基线，纳入需求、接口、测试和复用边界。 |
