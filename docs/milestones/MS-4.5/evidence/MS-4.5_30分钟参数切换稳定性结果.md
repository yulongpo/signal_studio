# MS-4.5 30 分钟参数切换稳定性结果

## 结论

2026-07-30 10:33:43～11:03:43，在 Windows 11、MSVC 2022 x64、
CPU Release（oneMKL）构建上连续执行 1,800 秒参数切换专项，进程退出码为 0，
标准错误为空，测试通过。

## 负载

- 在四组真实分析快照间轮换，覆盖 Periodogram/Welch、线性平均、指数平均、
  最大保持、移动平均、高斯和 Savitzky–Golay 平滑；
- 同时切换 FFT/STFT 帧长、FFT 长度、窗函数、重叠、补零、显示范围、
  色图、插值、时间缩放和频率缩放；
- 每轮执行真实分析、缓存查询、`ViewRequestId` 过期结果拒绝和最新结果提交；
- 完成 125,061 轮，缓存命中 125,057 次，主动构造并拒绝过期提交 7,357 次。

## 资源采样

以 30 秒间隔采样，共 60 个运行中样本：

| 指标 | 最小值 | 最大值 | 末次采样 |
|---|---:|---:|---:|
| 工作集 | 6,443,008 B | 21,778,432 B | 21,749,760 B |
| 私有内存 | 1,212,416 B | 14,467,072 B | 14,397,440 B |
| 句柄数 | 78 | 82 | 82 |
| 线程数 | 4 | 6 | 4 |

最后 12 个样本覆盖 10:57:44～11:03:14，私有内存保持
14,098,432～14,397,440 B，句柄固定为 82，线程固定为 4；不存在持续资源增长、
无界任务堆积、崩溃或半成品发布。

## 原始证据

- `ms45_parameter_stability_30min.stdout.log`：迭代与通过结果；
- `ms45_parameter_stability_30min.stderr.log`：0 B；
- `ms45_parameter_stability_30min_resources.csv`：60 组资源采样；
- `ms45_parameter_stability_runner.stdout.log`：退出码与资源极值；
- `ms45_parameter_stability_runner.stderr.log`：0 B。

完整 8 小时 MS-04 稳态不在本里程碑重复执行；原脚本和既有证据未修改，MS-4.5
新增专项不改变原稳态入口。
