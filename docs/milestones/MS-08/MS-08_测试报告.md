# MS-08 测试报告

日期：2026-07-23
里程碑：MS-08 复用证明应用
结论：通过。四配置全量 CTest 通过，signal_review 仅链接公共模块并在真实 1GB SC16 上有界读取统计。

## 1. 构建/测试矩阵

| 配置 | CTest |
|---|---:|
| 无界面 Debug | 130/130，通过 |
| 无界面 Release | 130/130，通过 |
| UI Debug（CUDA） | 164/164，通过 |
| UI Release（CUDA） | 164/164，通过 |

合计 588/588 通过。每配置比 MS-07 多 4 个 review 用例。

## 2. 复用审计

`signal_review` 链接目标（`apps/signal_review/CMakeLists.txt`）：
```
SignalStudio::Data SignalStudio::DSP SignalStudio::Compute SignalStudio::Core
```
不链接 Qt、Visualization、Workbench、TaskRuntime、PluginSDK、ModelRuntime、Dataset、app-core。证明十个公共模块可独立复用于非 GUI 工具。

## 3. Review 用例

| 用例 | 验证 | 结果 |
|---|---|---|
| `review.review-wav` | WAV 导入、complex IQ、有界 8192、magnitudeMean | 通过 |
| `review.review-sc16` | 无 sr 提示的 SC16 诚实报错（不伪造） | 通过 |
| `review.review-version` | --version 报告 app + platform | 通过 |
| `review.review-missing` | 缺失文件报 "file not found" | 通过 |

## 4. 真实数据复用

`signal_review` 在外部 1GB SC16（`x310_capture_cf1245MHz_sr50MSps_...sc16`）上：
- 文件名提示导入（cf=1245MHz, sr=50MSps）
- totalSamples=249,693,612，reviewedSamples=8192（有界）
- magnitudeMean=8921.73, rms=9980.82, peak=28562.01

证明公共模块在真实大数据上有界读取与统计正确，不读全文件。

## 5. 最终验证结论

| 验证项 | 结果 |
|---|---|
| 四配置全量 CTest | 588/588，通过 |
| 公共模块复用 | signal_review 仅链接 4 公共模块 |
| 真实数据 | 1GB SC16 有界读取统计通过 |

MS-08 验证通过；本结论不表示整体产品完成，下一阶段进入 MS-09（最终发布验证与发布）。
