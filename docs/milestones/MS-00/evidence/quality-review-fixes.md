# MS-00 质量审查修复证据

日期：2026-07-22

结论：五项审查问题均已形成可执行回归并通过本地、远程与最终独立复审验证。

## 问题、根因与修复

| 项目 | 失败证据/根因 | 修复与回归 |
|---|---|---|
| 依赖锁不可移植 | 默认校验把真实工具与开发机 `D:\` 路径、补丁版本和已安装文件哈希逐项比较，其他兼容主机必然失败 | 拆为 `Acquisition`、`CompatibleHost`、`ExactCapturedHost`；注入测试证明兼容补丁和替代路径通过，越界版本、错误工具族、x86 和精确模式路径变化被拒绝 |
| VS Code 构建树不一致 | 设置使用本机预设，任务使用源预设，F5 依赖未验证的当前 CMake 目标；明确平台测试目标首次直接运行还因 Qt DLL 不在进程 PATH 失败，退出码 `-1073741515` | 配置/构建/测试/F5 统一到 `local-windows-msvc-debug`；前置校验缓存、生成器、UI 选项、目标与输入新鲜度；构建后复制目标运行库，随后普通 PowerShell 直接启动通过 |
| Windows 根路径被破坏 | 定向测试复现 `C:\` 被规范化为 `C:`；相同尾裁剪逻辑也会破坏 UNC 根 | 根路径先与 `GetPathRoot()` 比较并保留终止分隔符，非根路径才裁剪；盘符、UNC、扩展长度根、重复项、大小写及排除项全部通过 |
| 公共枚举可注入未知值 | 失败优先编译测试确认缺少 `ModuleId` 与 `CapabilityAvailability` 已知值谓词 | 增加显式 `is_known_*`，注册表和模块描述符在修改状态前拒绝未知值；覆盖未知自身/依赖模块、未知能力可用性和合法空能力列表 |
| Status 满容量传播抛异常 | 第九次 `with_context()` 先压入第九层，再调用会拒绝深度大于八的工厂，违反传播 API 的正常边界行为 | 满容量时保留根因，淘汰最旧中间上下文，再附加最新上下文；第七、八、九次传播均保持有效且不抛异常 |

## 本地验证结果

- 同进程全新 Debug：45/45，CTest 21.84 秒，总耗时 37.760 秒。
- 同进程全新 Release：45/45，CTest 20.66 秒，总耗时 31.397 秒。
- 主矩阵合计：90/90；PATH 52/52 唯一，长度 3139。
- VS Code 本机 Debug 树：45/45；动态同树校验通过；`signal_studio_platform_tests.exe --case core.version` 直接启动通过。
- 依赖模式：实际 `Acquisition`、`CompatibleHost`、`ExactCapturedHost` 均通过；注入式正负用例通过。
- 完整性：BL1.0、外部数据、最小夹具、公共头文件、依赖锁、便携配置、全组件包消费者和独立无 Qt 包消费者均通过。

## 后续收口结果

- 历史远程运行 `29919175820` 仅覆盖旧实现提交，不作为质量修复提交的 CI 结论。
- 质量修复提交 `a1c252f873a01fb6ae3a7b0b9e1f60553341b171` 已由远程运行 `29924612586` 验证；Windows/Ubuntu 无界面作业和 Qt Windows UI 作业全部成功。
- 最终独立代码质量复审未发现仍需处理的严重或重要问题。精确结论见 `final-independent-reviews.md`；本文不记录或推断负责人身份。
