# MS-03 测试报告

日期：2026-07-23
里程碑：MS-03 SignalVisualization 与 SignalWorkbench
结论：通过。四配置全量 CTest 通过，可视化控件经 offscreen Qt 真实构造与绑定，无失败、无禁用、无伪装。

## 1. 构建/测试矩阵

| 配置 | 预设 | UI/CUDA | CTest |
|---|---|---|---:|
| 无界面 Debug | `local-windows-msvc-headless-debug` | OFF/OFF | 118/118，通过 |
| 无界面 Release | `local-windows-msvc-headless-release` | OFF/OFF | 118/118，通过 |
| UI Debug | `local-windows-msvc-debug` | ON/AUTO->ON | 142/142，通过 |
| UI Release | `local-windows-msvc-release` | ON/AUTO->ON | 142/142，通过 |

UI 配置比无界面多 24 个用例：12 个 visualization/workbench 用例 + 12 个 MS-02 CUDA/UI 用例。无界面配置不含 Visualization/Workbench（UI off）。

## 2. Visualization 用例（仅 UI 构建）

| 用例 | 验证 | 结果 |
|---|---|---|
| `viewport-window-clamp` | set_window/反转拒绝/越界钳位 | 通过 |
| `viewport-zoom-pan` | zoom 半窗、pan 钳位、reset 全窗 | 通过 |
| `frequency-unit` | Hz/kHz/MHz/GHz 自适应 | 通过 |
| `format-frequency` | Hz 级精度格式化 | 通过 |
| `overlay-model` | 游标/选区/测量/方向校验 | 通过 |
| `data-series` | 4 种系列 point_count/字段 | 通过 |
| `color-map` | grayscale 端点、viridis 端点差异 | 通过 |
| `view-factory` | 5 工厂构造、bind、native_widget、隐藏停止 | 通过 |

## 3. Workbench 用例（仅 UI 构建）

| 用例 | 验证 | 结果 |
|---|---|---|
| `service-registry` | register/resolve/空 id 拒绝 | 通过 |
| `command-registry` | register/invoke/未知失败/排序 id | 通过 |
| `panel-factory` | register/create/未知失败/区域 | 通过 |
| `diagnostics` | compute/cuda/panels/qt_version/notes | 通过 |

## 4. 交互不变量验证

- 隐藏停止计算：`view-factory` 验证 `set_visible(false)` 后 `needs_data()` 返回 false（API-VIS-003/004 要点 5）。
- 共享频率轴：`FrequencyViewport` 经 `ViewportController` 共享（API-VIS-002/003 要点 8）。
- 时间导航=实际读入长度：`TimeViewport::LoadedDataRange`（API-VIS-004 要点 1）。
- 频率单位自适应 + Hz 级精度：`frequency-unit`/`format-frequency`（要点 17/18）。
- 选区方向校验：`overlay-model`（要点 13/14）。

## 5. 质量门禁

`/W4 /permissive- /EHsc /Zc:__cplusplus /utf-8` 通过，无新增警告。公共头无 QWidget/Qt 类型（`native_widget()` 返回 `void*`）。依赖 DAG 校验通过。offscreen Qt 经 `QT_QPA_PLATFORM=offscreen` + `QT_PLUGIN_PATH` 提供，不产生窗口截图（任务约束）。

## 6. 最终验证结论

| 验证项 | 结果 |
|---|---|
| 无界面 Debug | 118/118，通过 |
| 无界面 Release | 118/118，通过 |
| UI Debug（CUDA） | 142/142，通过 |
| UI Release（CUDA） | 142/142，通过 |
| 本地测试合计 | 520/520，通过 |
| Visualization/Workbench 控件 | offscreen 构造+绑定验证通过 |
| 工作树（收口提交前） | 待收口提交后干净 |

MS-03 验证通过；本结论不表示整体产品完成，下一阶段进入 MS-04（Signal Studio 基础应用）。
