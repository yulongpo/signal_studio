# MS-03 本地最终验证证据

## 1. 构建与测试

2026-07-28 在 `D:\coding\signal_studio_prj\signal_studio_dev`、分支
`codex/full-signal-studio-development` 执行：

| 预设 | 配置 | 编译 | CTest |
|---|---|---|---:|
| `local-windows-msvc-cpu-debug` | 通过 | 通过 | 全量 198/198 |
| `local-windows-msvc-cpu-release` | 通过 | 通过 | 全量 198/198 |
| `local-windows-msvc-cuda-debug` | 通过 | 通过 | MS-03 集合 59/59 |
| `local-windows-msvc-cuda-release` | 通过 | 通过 | MS-03 集合 59/59 |
| `local-windows-msvc-headless-debug` | 通过 | 通过 | 全量 133/133 |
| `local-windows-msvc-headless-release` | 通过 | 通过 | 全量 133/133 |

安装消费者在六个预设中各独立重装、配置、编译和运行，均为 1/1。UI 安装路径强制要求演示程序存在，再清空
`QT_QPA_PLATFORM`、`QT_PLUGIN_PATH` 和 `QT_QPA_PLATFORM_PLUGIN_PATH` 启动，不允许因文件缺失而跳过。

最终 `.ui` 对象名清理后，CPU Debug 重新编译并执行 56 项 MS-03 加一项安装消费者，57/57 通过。

## 2. 工具链

- MSVC：19.44.35228 x64；
- CMake：4.3.1；
- Ninja：1.12.1；
- Qt：6.11.1 `msvc2022_64`；
- CUDA Toolkit：12.4.131；
- GPU：NVIDIA GeForce RTX 5060 Laptop GPU；
- 驱动：591.84。

不执行 Ubuntu 24.04，不安装 CUDA 或 cuDNN。

## 3. 质量与边界

- `clang-format`：12/12；
- `clang-tidy`：四个生产实现均退出码 0、0 errors，warning occurrence 为 7、7、9、9；
- `git diff --check`：通过；
- 不可变基线：136/136；
- 外部材料：4/4；
- 最小夹具：4/4；
- 公共头：29/29；
- 依赖锁：14 个不可变元组、1 个扩展、8 个获取契约、2 个离线制品；
- 可移植配置：5/5；
- VS Code 工作流：通过；
- Windows-only GitHub Actions 静态契约：通过。

## 4. 本机用户预设

连续两次生成 SHA-256 均为
`5492c98be299cf0348d0e4d101f536df269786afe0e428da257b28bbcb0456b7`。PATH 为 57/57 唯一项，
INCLUDE 为 7/7，LIB 为 3/3，LIBPATH 为 5/5，`CMAKE_PREFIX_PATH` 为 1/1；重复项均为 0。

## 5. UI 制品

四张截图均由实际 Qt 演示程序自动生成并核验非空：

- 1280×720 逻辑窗口；
- 1600×900 逻辑窗口；
- 1920×1080 逻辑窗口；
- 200% DPI。

详细物理像素、SHA-256 和打开说明见
`docs/development/ui-preview/MS-03_UI预览索引.md`。
