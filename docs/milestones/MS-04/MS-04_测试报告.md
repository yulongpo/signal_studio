# MS-04 测试报告

日期：2026-07-23
里程碑：MS-04 Signal Studio 基础应用
结论：通过。四配置全量 CTest 通过，应用导入/读取/分析闭环经最小夹具与真实外部数据验证，self-test 无头通过，无失败、无禁用、无伪装。

## 1. 构建/测试矩阵

| 配置 | 预设 | UI/CUDA | CTest |
|---|---|---|---:|
| 无界面 Debug | `local-windows-msvc-headless-debug` | OFF/OFF | 118/118，通过 |
| 无界面 Release | `local-windows-msvc-headless-release` | OFF/OFF | 118/118，通过 |
| UI Debug | `local-windows-msvc-debug` | ON/ON | 150/150，通过 |
| UI Release | `local-windows-msvc-release` | ON/ON | 150/150，通过 |

UI 配置比无界面多 32 个用例：MS-03 的 24 个 + MS-04 的 8 个应用用例（7 个 app.* + self-test）。无界面配置不含应用（UI off）。

## 2. 应用用例（仅 UI 构建）

| 用例 | 验证 | 结果 |
|---|---|---|
| `app.filename-parse` | SC16 文件名 cf1245MHz/sr50MSps 解析、单位换算 | 通过 |
| `app.import-wav` | WAV 自动解析、complex IQ、total_samples>0、有界读取 | 通过 |
| `app.import-sc16-override` | SC16 覆盖参数导入、int16/interleaved、有界读取 | 通过 |
| `app.import-sc16-no-sr-fails` | 无 sr 提示/覆盖时拒绝 | 通过 |
| `app.analyze-psd` | PSD 分析、frame_count>0、CUDA provenance | 通过 |
| `app.external-wav` | 外部 20MB WAV（12.8MSps）有界 8192 样本读取 | 通过 |
| `app.external-sc16` | 外部 1GB SC16 文件名提示导入、cf/sr、有界 8192 样本读取 | 通过 |
| `app.self-test` | `signal_studio --self-test` 无头导入/读取最小夹具 | 通过 |

外部数据用例在数据缺失时返回 SKIP（exit 0 + stderr 标记），不误报通过。

## 3. 真实数据边界

- 外部 WAV（20,480,044 字节，12.8 MSps，5.12M 帧）：有界窗口读取 8192 样本，未读全文件。
- 外部 SC16（998,774,448 字节，50 MSps，1245 MHz）：文件名提示导入，有界窗口读取 8192 样本，未读全文件。
- 第二个 SC16（4GB，1425 MHz）未单独测（同路径，文件名提示逻辑已由第一个覆盖）。

## 4. 无头 self-test

`signal_studio.exe --self-test` 在无 QApplication 下运行 Qt-free 导入/读取路径，输出"imported 1024 samples @ 64000.000 Hz, read 1024"，退出码 0。CI 可直接用此入口验证应用组合正确性。

## 5. 质量门禁

`/W4 /permissive- /EHsc /Zc:__cplusplus /utf-8` 通过，无新增警告。应用仅链接公共库（Data/DSP/Compute/TaskRuntime/Visualization/Workbench/Core），不直接调用 cuFFT/Qt 私有 API。公共 API 不暴露 QWidget。依赖 DAG 校验通过。

## 6. 最终验证结论

| 验证项 | 结果 |
|---|---|
| 无界面 Debug | 118/118，通过 |
| 无界面 Release | 118/118，通过 |
| UI Debug（CUDA） | 150/150，通过 |
| UI Release（CUDA） | 150/150，通过 |
| 本地测试合计 | 536/536，通过 |
| 应用导入/读取/分析 | 最小夹具+外部数据验证通过 |
| 无头 self-test | 通过 |

MS-04 验证通过；本结论不表示整体产品完成，下一阶段进入 MS-05（宽窄带联动分析）。
