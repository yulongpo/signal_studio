# MS-09 计划

## 1. 目标

执行最终验收矩阵、合并默认分支、创建版本标签、推送、创建 GitHub Release（或记录阻塞）、生成最终执行报告。

## 2. 实施

1. 最终验收报告 `docs/release/FINAL_ACCEPTANCE_REPORT.md`（§18 矩阵 35 项）。
2. 最终执行报告 `docs/release/最终执行报告.md`（§19 28 节）。
3. 合并 `claude/GLM-sig-studio-dev` 到 `main`。
4. 标签 `v1.0.0`。
5. 推送 main + 标签 + dev 分支。
6. GitHub Release：gh CLI 不可用为阻塞，生成命令。

## 3. 验证

- 四配置全量 CTest 通过（588/588）。
- 便携包生成并可启动。
- 工作树干净。
