# MS-07 实施记录

日期：2026-07-23
里程碑：MS-07 工程化、打包、文档

## 1. 交付内容

### 1.1 质量门禁（`scripts/run_quality_gates.ps1`）

- clang-format `--dry-run --Werror` 检查（VS BuildTools 自带 LLVM clang-format）；`-FormatCheck` 模式失败退出，默认模式自动格式化。已格式化全部 111 个源文件。
- 编译高警告级别 `/W4 /permissive- /EHsc /Zc:__cplusplus /utf-8` 由 `signal_studio_enable_warnings` 对每个目标强制。
- 依赖 DAG 由 `signal_studio_verify_dependency_graph` 在配置期强制。
- 公共头第三方类型扫描由 `tests/platform` 契约测试覆盖。

### 1.2 打包（`cmake/Packaging.cmake`、`apps/signal_studio/CMakeLists.txt`）

- CPack ZIP 便携包 + NSIS（若 makensis 可用）。
- windeployqt POST_BUILD 部署 Qt DLL/插件（platforms/styles/imageformats/iconengines/tls/generic）到 `signal_studio.exe` 同目录。
- VC143 运行库由 `signal_studio_deploy_msvc_runtime` 部署。
- `install(TARGETS signal_studio ... COMPONENT applications)` + runtime/headers/cmake 组件。
- 便携包 `SignalStudio-1.0.0-win64.zip`（25MB，122 文件，SHA256 `bcc7f1ba...`），解压即用，self-test 退出 0。

### 1.3 需求追踪（`docs/development/开发需求追踪矩阵.csv`）

198 项（160 FR + 38 NFR），按 ID 前缀映射里程碑与模块，MS-01~06 已实现标记"通过"。

### 1.4 文档

- `docs/development/VSCode构建与调试指南.md`：工具链/预设/.vscode/工作流/常见问题。
- `docs/development/第三方依赖版本锁定清单.md`：实际使用 4 项 + 声明未装 8 项偏差。
- `docs/release/发布检查清单.md`、`docs/release/安装验证报告.md`、`docs/release/ReleaseNotes_1.0.0.md`。
- `LICENSES/LICENSE.txt`：MIT 源码 + Qt/MSVC 运行库许可说明。

## 2. 修改文件清单

新增：`cmake/Packaging.cmake`、`scripts/run_quality_gates.ps1`、`LICENSES/LICENSE.txt`、`docs/development/{VSCode构建与调试指南,第三方依赖版本锁定清单}.md`、`docs/development/开发需求追踪矩阵.csv`、`docs/release/{发布检查清单,安装验证报告,ReleaseNotes_1.0.0}.md`、MS-07 计划/实施/测试/完成文档。
修改：`CMakeLists.txt`（include Packaging）、`apps/signal_studio/CMakeLists.txt`（windeployqt + 组件 install）、全部 111 源文件（clang-format 格式化）。

## 3. 架构与依赖变化

无架构变化。无新增第三方依赖。Packaging 仅 UI 构建启用。

## 4. 验证命令

```bash
pwsh -NoProfile -File scripts/run_quality_gates.ps1 -FormatCheck   # 格式合规
cmake --preset local-windows-msvc-release && cmake --build --preset local-windows-msvc-release
ctest --preset local-windows-msvc-release
cd build/local-windows-msvc-release && cpack -C Release            # 便携包
```

## 5. 已知问题与边界

- NSIS 未装：仅便携包，安装器为环境偏差。
- vcpkg manifest 未启用（Qt 冲突）。
- CUDA 不自动安装。
