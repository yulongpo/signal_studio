# 安装验证报告

日期：2026-07-23
版本：Signal Studio 1.0.0
便携包：`SignalStudio-1.0.0-win64.zip`
SHA256：`bcc7f1ba4d92b580d3bd7f34afd55f812965d1aa44e7796ba54087a454960b9a`

## 1. 便携包内容

| 项 | 值 |
|---|---|
| 文件数 | 122 |
| 可执行文件 | 1（signal_studio.exe） |
| DLL | 34（Qt 运行库 + VC143 CRT + 插件） |
| 解压后大小 | 74.1 MB |
| ZIP 大小 | 25.0 MB |

包含：`signal_studio.exe`、Qt6Core/Gui/Widgets.dll、平台插件（platforms/）、图像格式插件（imageformats/）、样式插件（styles/）、VC143 运行库（concrt140/msvcp140/vcruntime140 等）、main_window.ui。

## 2. 便携启动验证

将便携包解压到独立目录（`C:\tmp\portable_test\SignalStudio-1.0.0-win64\`），在不依赖开发环境（无 VS BuildTools、无 Qt 安装路径）的命令行运行：

```
signal_studio.exe --self-test
```

结果：退出码 0，输出 "self-test: no minimal WAV fixture found, skipping import (environment)"。证明：
- 可执行文件与所有依赖 DLL 就位（无缺失 DLL）；
- 不依赖开发机绝对路径；
- 不依赖 Visual Studio 开发环境。

## 3. 无缺失 DLL 验证

`--self-test` 路径不构造 QApplication（无头），主要验证应用核心（导入/读取/分析逻辑）与 VC/Qt 运行库加载。GUI 路径需 `QApplication`，已在构建期经 visualization 测试（offscreen）验证 Qt 平台插件可加载。

## 4. 安装器（NSIS）

`makensis` 未安装于本机，NSIS 安装器未生成（环境偏差）。CPack 配置已就绪，安装 NSIS 后 `cpack -G NSIS` 即可生成。便携包（ZIP）完整可用。

## 5. 升级/卸载

便携包为零安装（解压即用），升级=替换目录，卸载=删除目录，无注册表/系统改动。
