# Signal Studio 开发计划

## 1. 项目状态

- 当前产品版本：V1.0 目标版本
- 当前需求基线：SS-SRS-BL-1.0，正式生效，只读
- 当前开发阶段：M1 宽带浏览基础
- 当前里程碑：M1
- 当前主要任务：先补齐构建与测试基础，再实施 RAW IQ 分块读取
- 最后更新时间：2026-07-15

## 2. 总体里程碑

### M1 宽带浏览基础

IQ 文件导入、RAW 格式配置、大文件分块访问、时域/频谱/STFT 显示、缩放和平移、游标和选区、基础缓存、基础任务调度、基础项目保存。

### M2 宽窄带联动闭环

信号区域、窄带通道、DDC、滤波、抽取、重采样、窄带图谱、双向定位和项目恢复。

### M3 专业分析能力

参数估计、星座图、眼图、调制识别、多通道对比、模型推理和结果导出。

### M4 工程化发布

性能优化、稳定性、崩溃恢复、安装包、用户文档和发布测试。

## 3. 当前迭代

- 目标：完成 M1 的最小可实现设计和首批技术预研定义。
- 范围：文件访问、坐标、缓存、渲染、任务调度、基础项目模型。
- 完成条件：形成可执行设计，明确待验证指标，并为实现和测试建立追踪入口。
- 关联需求：SS-BIZ-001、SS-BIZ-007、SS-WB-001、SS-WB-002、SS-WB-003、SS-NFR-002、SS-NFR-003、SS-NFR-005。
- 预计输出：架构文档、决策记录、M1 测试清单和下一步实现任务。

## 4. 正在进行

- [ ] 定义超大 IQ 文件分块读取方案
  - 需求：SS-BIZ-007、SS-NFR-002、SS-NFR-006
  - 模块：core/data
  - 测试：分块读取、边界、吞吐和内存测试
  - 状态：待设计
- [ ] 定义时间/频率坐标和宽带视图数据模型
  - 需求：SS-ACC-001、SS-ACC-002
  - 模块：core/data、visualization
  - 测试：坐标映射和误差回归
  - 状态：待设计
- [ ] 评估多分辨率索引与 STFT 缓存
  - 需求：SS-WB-003、SS-NFR-004
  - 模块：core/cache
  - 测试：命中率、缓存体积、损坏恢复
  - 状态：待预研
- [ ] 明确图谱渲染与 UI 线程边界
  - 需求：SS-NFR-001、SS-NFR-003
  - 模块：visualization
  - 测试：首帧、FPS、交互响应
  - 状态：待预研
- [ ] 定义可取消任务模型
  - 需求：SS-NFR-005
  - 模块：core/task
  - 测试：取消、过期结果和资源回收
  - 状态：待设计

## 5. 下一步任务

1. 确认构建与测试基础，并创建最小 CMake/CTest 入口（仅在源码结构确认后）。
2. 创建确定性 RAW IQ 测试数据生成器和 manifest。
3. 定义 RAW IQ 格式模型、文件检查和 64 位溢出检查。
4. 实现同步分块读取核心并覆盖 TEST-DATA-002 至 TEST-DATA-009。
5. 完成边界、错误、内存和重复随机访问验证。

## 6. 已完成

- 正式需求基线 BL1.0 已归档并保留 SHA-256 校验值。
- 简化后的开发文档入口已建立。
- `.gitignore` 已建立并提交。

## 7. 阻塞问题

| 问题 | 影响 | 处理方案 | 状态 |
|---|---|---|---|
| 当前仓库尚无可确认的源码模块结构 | 无法指定实现文件和编译命令 | 开始编码前检查源码目录与构建入口 | 开放 |
| 性能基准文件和参考硬件未确定 | 无法执行性能验收 | 补充测试数据与环境后执行 | 开放 |

## 8. 风险

- 大文件访问性能
- 图谱渲染性能
- STFT 缓存体积
- 宽窄带状态一致性
- DSP 数值正确性
- Python/CUDA 环境
- V1.0 范围膨胀

## 9. 最近验证结果

- 已验证 BL1.0 正式文件移动前后 SHA-256 一致。
- 已验证活动 RTM 行数为 148，需求 ID 无重复。
- 已执行 `cmake --version`，CMake 4.3.1 可用。
- 已执行 `cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug`，因缺少 `CMakeLists.txt` 失败。
- 已执行 `ctest --test-dir . --output-on-failure`，结果为 `No tests were found!!!`。
- 尚未执行源码编译或软件测试。

## 10. Codex 下一轮任务

M1-DATA-001：构建与测试基础确认。确认源码目录、补齐最小 CMake/CTest 入口并运行一个基础测试；不得实现 RAW IQ 读取功能，不修改正式需求基线。

## 11. M1 RAW IQ 实施任务拆分

| 任务 | 关联需求 | 目标与输入/输出 | 预计修改文件 | 测试 | 前置依赖 | 完成条件 | 状态 |
|---|---|---|---|---|---|---|---|
| M1-DATA-001 | 构建基础，SS-NFR-010 | 确认源码目录、CMake/CTest 和基础测试目标 | `CMakeLists.txt`、`tests/`（仅确认后创建） | 配置、构建、基础测试 | 无 | 测试目标可构建并执行 | 待开始 |
| M1-DATA-002 | SS-BIZ-007、SS-NFR-006 | 建立确定性生成器、manifests 和 SHA-256 | `tests/test_data/`、`.gitignore`（必要时） | 重复生成一致 | M1-DATA-001 | 样本可重复生成，大型文件不入库 | 待开始 |
| M1-DATA-003 | SS-BIZ-007、SS-NFR-012 | 实现数据类型、IQ/QI、字节序、头偏移和溢出检查 | `src/core/data/`、`tests/unit/` | 合法/非法文件 | M1-DATA-001、002 | 合法和非法输入均有测试 | 待开始 |
| M1-DATA-004 | SS-BIZ-007、SS-ACC-005 | 实现指定样本范围的同步分块读取 | `src/core/data/`、`tests/unit/` | TEST-DATA-002 至 009 | M1-DATA-003 | 支持 Int8/Int16/Float32、IQ/QI、两种字节序 | 待开始 |
| M1-DATA-005 | SS-NFR-006、SS-NFR-012 | 验证文件尾、越界、溢出、内存和重复访问 | `tests/unit/`、性能测试配置 | TEST-DATA-008 至 013 | M1-DATA-004 | 边界和内存约束有实际证据 | 待开始 |
| M1-DATA-006 | SS-NFR-001、SS-NFR-005 | 准备不依赖 QWidget 的稳定调用入口 | `src/core/data/`、接口测试 | 核心模块独立使用 | M1-DATA-004、005 | 核心读取可供应用层接入 | 待开始 |
