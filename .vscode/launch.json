{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Signal Studio MS-00 platform tests",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "environment": [],
      "console": "integratedTerminal",
      "preLaunchTask": "Signal Studio: Build Debug"
    }
  ]
}
